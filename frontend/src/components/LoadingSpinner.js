import React from 'react';
import { LoadingSpinnerStyle, LoadingContainer } from '../styles';

const LoadingSpinner = () => {
    return (
        <LoadingContainer>
            <LoadingSpinnerStyle />
        </LoadingContainer>
    );
};

export default LoadingSpinner;